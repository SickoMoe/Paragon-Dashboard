export const relationships = ['property_owner', 'co_owner', 'authorized_representative', 'executor', 'agent_broker', 'entity_representative', 'other'];
export const categories = ['proof_authority', 'title', 'survey', 'seller_disclosure', 'inspection', 'hoa', 'appraisal', 'tax', 'legal', 'auction', 'other'];
const labels: Record<string, string> = { proof_authority: 'Proof of authority', seller_admin: 'Seller & operations only', internal: 'Operations only', approved_bidders: 'Approved bidders', public: 'Public viewers', needs_information: 'More information needed', not_submitted: 'Not submitted', agent_broker: 'Agent / broker', executor: 'Executor / estate representative' };
export const label = (value: string) => labels[value] ?? value.replace(/_/g, ' ').replace(/\b\w/g, c => c.toUpperCase());
export type Authority = { relationship: string; legalName: string; representativeName: string; contact: string; brokerage: string; notes: string; status: string; reviewNote?: string; correctionReason?: string };
export type Document = { documentId: string; filename: string; category: string; visibility: string; status: string; uploadedAt: string; note: string; reviewNote?: string; canRemove: boolean };
export type Liquidation = { listingId: string; revision: number; authority: Authority; documents: Document[]; requiredDocuments: string[]; requirementsNote: string; canReview: boolean; authorityLocked: boolean; authorityLockReason: string | null; canEditAuthority: boolean; listingReviewStatus: string; auctionStatus: string; readiness: { ready: boolean; documentsReady: boolean; checks: { code: string; label: string; satisfied: boolean }[]; blockers: { code: string; label: string }[] } };
